#include <stdio.h>

/**
 *main - Enter points
 *
 *Return: Always 0 (success)
 */

int main(void)
{
	char c;

	c ='a';
	while 
		(c <= 'z') {
			putchar(c);
			c++;
		}
	while
		(d <= 'z') {
			putchar(c);
			d++;
		}
	putchar('\n');
	return (0);
}
